# Configuration de déploiement pour l'environnement de staging

# Configuration SSH
SSH_KEY="$HOME/.ssh/id_rsa"  # Chemin vers votre clé SSH privée
SSH_USER="utilisateur"        # Utilisateur SSH sur le serveur
SSH_HOST="staging.example.com" # Adresse du serveur de staging

# Répertoire cible sur le serveur
TARGET_DIR="/var/www/gw2-wvwbuilder-staging"

# Configuration de l'application
WORKERS=4
THREADS=2
TIMEOUT=120

# Configuration de la base de données
DATABASE_URL="postgresql://user:password@db-host:5432/gw2_wvwbuilder_staging"
REDIS_URL="redis://redis-host:6379/0"

# Clé secrète pour l'application (à générer avec: openssl rand -hex 32)
SECRET_KEY="votre_clé_secrète_ici"

# Mode débogage (True/False)
DEBUG="True"
